# Adding ubuntu-lxc stable repo
if [ -z "$(which go)" ]; then
  echo Adding ubuntu-lxc stable repo
  sudo add-apt-repository ppa:ubuntu-lxc/lxd-stable
  sudo apt-get update
  sudo apt-get install golang
fi
